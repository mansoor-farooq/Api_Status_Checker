<!-- <?xml version="1.0" encoding="UTF-8"?>
<configuration>
  <system.webServer>

    <!-- React Router Rewrite Rule -->
    <rewrite>
      <rules>
        <rule name="ReactRoutes" stopProcessing="true">
          <match url=".*" />
          <conditions logicalGrouping="MatchAll">
            <!-- Agar requested file exist nahi karti -->
            <add input="{REQUEST_FILENAME}" matchType="IsFile" negate="true" />
            <!-- Agar requested folder exist nahi karta -->
            <add input="{REQUEST_FILENAME}" matchType="IsDirectory" negate="true" />
          </conditions>
          <!-- To sab requests index.html pe forward ho jaaye -->
          <action type="Rewrite" url="/index.html" />
        </rule>
      </rules>
    </rewrite>

    <!-- Static File Caching (Performance) -->
    <staticContent>
      <!-- Files ko 30 din ke liye cache karega -->
      <clientCache cacheControlMode="UseMaxAge" cacheControlMaxAge="30.00:00:00" />
    </staticContent>

    <!-- Security Headers -->
    <httpProtocol>
      <customHeaders>
        <add name="X-Content-Type-Options" value="nosniff" />
        <add name="X-Frame-Options" value="DENY" />
        <add name="X-XSS-Protection" value="1; mode=block" />
        <add name="Referrer-Policy" value="strict-origin-when-cross-origin" />
        <add name="Content-Security-Policy" 
             value="default-src 'self'; script-src 'self'; style-src 'self' 'unsafe-inline'; img-src 'self' data:; font-src 'self' data:;" />
      </customHeaders>
    </httpProtocol>

  </system.webServer>
</configuration> -->
